# TODO

- [ ] Fix `frontend/src/pages/Home.jsx` structure (remove embedded duplicate code/imports, ensure valid JSX, render `FloatingProductShowcase` correctly, single final `export default Home`).
- [ ] Run frontend build/dev to verify no import/parse errors.

